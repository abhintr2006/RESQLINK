# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.141.1,<1.0
uvicorn[standard]>=0.52.4,<1.0
pydantic>=2.13.4,<3.0
pydantic-settings>=2.15.0,<3.0

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.9,<3.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.52,<3.0
alembic>=1.19.1,<2.0
aiosqlite>=0.20,<1.0          # SQLite async driver (local dev default)
# asyncpg>=0.29,<1.0          # Uncomment for PostgreSQL

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi>=0.1.9,<1.0

# ── Logging ───────────────────────────────────────────────────────────────────
structlog>=24.0,<25.0

# ── SMS (optional) ────────────────────────────────────────────────────────────
# twilio>=9.0,<10.0           # Uncomment when TWILIO_* env vars are set

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0
anyio>=4.0,<5.0
